# Local: tutor que arrancas tú. Sin portero ni secreto.
BACKEND_URL=http://localhost:8000
# Vercel (Secrets): BACKEND_URL=https://….ngrok-free.dev y BACKEND_WAKE_KEY=BOOT_SECRET
# Vacío en local = sin pantalla de PIN. En Vercel: Secret GATE_PASSWORD, PIN de 6 dígitos (no NEXT_PUBLIC_).
GATE_PASSWORD=
