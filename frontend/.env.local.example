# Solo el proceso de Next. Nunca NEXT_PUBLIC_: el túnel y el secreto no van al móvil.
BACKEND_URL=http://localhost:8000
# En Vercel (Secret), mismo valor que BOOT_SECRET del Mac. En local déjalo vacío.
# BACKEND_WAKE_KEY=
